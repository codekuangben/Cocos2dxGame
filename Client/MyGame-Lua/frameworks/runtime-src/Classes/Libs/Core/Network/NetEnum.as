package network 
{
	/**
	 * ...
	 * @author ...
	 * @brief 网络常量定义的地方 
	 */
	public class NetEnum 
	{
		// 服务器那边压缩定义 
		// static const unsigned int PACKET_ZIP 0x40000000；
		// 服务器定义的压缩包的最小定义 
		// static const unsigned int PACKET_ZIP_MIN = 32;
		public static const PACKET_ZIP:uint = 0x40000000;
		public static const PACKET_ZIP_MIN:uint = 32;
	}
}